# Copy to `.env` and fill in from Firebase Console → Project settings → Your apps → Web app.
# Never commit `.env` (it is gitignored).

REACT_APP_FIREBASE_API_KEY=
REACT_APP_FIREBASE_AUTH_DOMAIN=
REACT_APP_FIREBASE_PROJECT_ID=
